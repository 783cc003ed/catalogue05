## DuckDuckgo search -> Windows integer Protocol
`2025-08-21`

* https://stackoverflow.com/questions/54010365/how-to-see-what-is-reserving-ephemeral-port-ranges-on-windows

<blockquote>
 HowtoseewhatisreservingephemeralportrangesonWindows
</blockquote>
<blockquote>
I have a Windows application that needs to use ports 50005 and 50006 but it is being blocked. I see the following when I run netsh int ip show excludedportrange protocol&#61;tcp: Protocol tcp Port
</blockquote>

---

## DuckDuckgo search -> Windows integer Protocol
`2025-08-21`

* https://renenyffenegger.ch/notes/Windows/development/WinAPI/data-types/index

<blockquote>
 WinAPIdatatypesandconstantsRenéNyffenegger
</blockquote>
<blockquote>
LARGE_INTEGER LARGE_INTEGER represents a 64-bit signed integer value. Compare with the (Microsoft extensions?) __int64 and hyper. LARGE_INTEGER is used (among others?) for QueryPerformanceCounter.
</blockquote>

---

## DuckDuckgo search -> Windows integer Protocol
`2025-08-21`

* https://win32.pub/docs/guides-concepts/integer-types

<blockquote>
 IntegerTypeswin32
</blockquote>
<blockquote>
Integer Types Unlike Dart, which has a single integer type for storing values between -263-1 and 263, the Win32 API utilizes various integer types in both signed and unsigned forms.
</blockquote>

---

## DuckDuckgo search -> Windows integer Protocol
`2025-08-21`

* https://learn.microsoft.com/en-us/windows/win32/api/winsock2/nf-winsock2-socket

<blockquote>
 socketfunctionwinsock2hWin32appsMicrosoftLearn
</blockquote>
<blockquote>
For the AF_NETBIOS address family, the protocol parameter is the LAN adapter number represented as a negative number. On Windows XP and later, the following command can be used to list the Windows Sockets catalog to determine the service providers installed and the address family, socket type, and protocols that are supported.
</blockquote>

---

## DuckDuckgo search -> Windows integer Protocol
`2025-08-21`

* https://learn.microsoft.com/en-us/openspecs/windows_protocols/ms-doco/c67743b6-4291-4dd4-baf2-60e5d2e4cb05

<blockquote>
 MSDOCOWindowsDataTypesMicrosoftLearn
</blockquote>
<blockquote>
Windows data types are common data types that are used in the Windows protocols documentation set. They are presented in the following document: [MS-DTYP]: Windows Data Types The Windows data types are categorized as follows: Common base types: Primitive data types, including IDL base types, which are natively supported by Microsoft compilers; for example, byte, handle_t, and wchar_t. Common ...
</blockquote>

---

